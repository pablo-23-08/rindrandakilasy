# public/ est le dossier racine du site (local ou hébergement type AlwaysData).
# Comme le routage se fait via index.php?route=..., aucune réécriture d'URL n'est nécessaire.

DirectoryIndex index.php

# Empêche l'affichage du contenu des dossiers
Options -Indexes
